# Importação do dataset Pokémon

Exercício acadêmico que lê `pokemon.csv` e insere os registros em uma tabela MySQL chamada `pokemonsinfo`.

Configure `MYSQL_PASSWORD` e crie previamente o banco `pokemons` e a tabela esperada pelo script.
