# Estudos de MySQL

Exercícios de conexão entre Python, Pandas e MySQL.

Defina a senha antes de executar:

```bash
export MYSQL_PASSWORD="sua-senha"
```

Também podem ser configuradas as variáveis `MYSQL_HOST`, `MYSQL_USER` e `MYSQL_DATABASE`. Os bancos e tabelas precisam ser criados conforme o exemplo utilizado.
